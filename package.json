{
  "name": "dongato",
  "version": "1.0.0",
  "description": "Descripción de tu proyecto",
  "main": "server/server.js",
  "scripts": {
    "build-client": "cd client && npm install && set NODE_OPTIONS=--openssl-legacy-provider && npm run build",
    "build-server": "cd server && npm install"
  },
  "author": "Adrian",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^6.0.2"
  }
}
